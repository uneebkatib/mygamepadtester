import React from 'react';
import GuidesClient from './Client';

export const metadata = {
    title: 'Gamepad & Controller Guides | MyGamepadTester',
    description: 'Complete guides for controller testing, calibration, drift detection, and hardware repair for Xbox, PlayStation, Nintendo, and more.',
};

export default function GuidesPage() {
    return <GuidesClient />;
}
